<h1>Fortunka #<%= @fortune.id %></h1>
<blockquote>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
  <%= @fortune.body %>
</blockquote>
<footer class="postmeta">
<span class="btn alignleft">
                	<%= link_to "Wszystkie fortunki", fortunes_path %>
				</span>
                <a class="more-link alignright">Tagi:&nbsp;&nbsp;&nbsp;<%= @fortune.tag_list %></a> 
            </footer> <!-- end post meta -->

<% if @fortune.comments.any? %>
  <h2>Komentarze</h2>
  <%= render @fortune.comments %>
<% end %>

<h2>Dodaj komentarz</h2>
<%= render 'comments/form' %>

